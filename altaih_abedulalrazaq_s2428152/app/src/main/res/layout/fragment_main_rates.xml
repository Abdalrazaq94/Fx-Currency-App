<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:fillViewport="true">


    <LinearLayout
        android:orientation="vertical"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- LAST UPDATED -->
        <TextView
            android:id="@+id/rssDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Last updated: --"
            android:textColor="#0A0B0B"
            android:background="#B9EDF4"
            android:padding="12dp"
            android:textSize="16sp"
            android:layout_marginBottom="10dp" />

        <!-- SEARCH BAR -->
        <EditText
            android:id="@+id/searchInput"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:hint="🔍 Search by currency or country name…"
            android:background="#EEEEEE"
            android:padding="12dp"
            android:layout_marginBottom="12dp"
            android:textColor="#000"
            android:textColorHint="#888" />

        <!-- TOP 3 TITLE -->
        <TextView
            android:id="@+id/topThreeTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="6dp" />

        <!-- TOP 3 RECYCLER -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/mainRatesRecycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:overScrollMode="never"
            android:layout_marginBottom="15dp" />

        <!-- FULL LIST TITLE -->
        <TextView
            android:id="@+id/fullListTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="6dp" />

        <!-- “NO RESULTS FOUND” MESSAGE -->
        <TextView
            android:id="@+id/noResultsText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No results found"
            android:textSize="16sp"
            android:textColor="#B00020"
            android:padding="8dp"
            android:visibility="gone"
            android:layout_marginBottom="6dp" />

        <!-- FULL LIST RECYCLER -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/allCurrenciesRecycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:overScrollMode="never" />

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
